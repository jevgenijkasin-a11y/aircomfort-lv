# Data lives in SQLite at DATA_DIR (default: ./data). No external services needed.
# Optional overrides:
# DATA_DIR=/absolute/path/to/data
# ADMIN_SECRET=<random string; overrides the DB-stored session secret>
